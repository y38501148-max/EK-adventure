# 开发文档

本项目使用 Godot 4.x 与 GDScript 构建，不使用纯前端 JS/TS 技术栈。

## 项目结构

```text
assets/        图像、音频、字体等资源
resources/     卡牌、效果、主题等静态定义
scenes/        Godot 场景
scripts/       GDScript 源码
tests/         后续测试目录
```

## 入口

- 主场景：`res://scenes/main/Main.tscn`
- 应用图标：`res://assets/art/icon.png`
- 基础运行框架：`res://scenes/game/GameRoot.tscn`

## 平台

项目配置面向桌面端，包含 Windows 与 macOS 导出预设骨架。正式打包前需要在本机 Godot 编辑器中安装对应导出模板，并补齐签名、公证、输出路径等平台细节。

## 当前框架

- `Settings`、`SceneRouter`、`SaveManager` 作为全局服务。
- `GameState` 保存回合、能量、牌堆、手牌、弃牌堆等运行时状态。
- `CardDefinition` 与 `CardEffect` 作为后续数据驱动卡牌的基础资源。
- `CardMarkdownLoader` 从 `cards-md/*.md` 读取卡牌草稿，当前可读取 `moni.md` 中的“枚举”。
- `MonsterMarkdownLoader` 从 `monsters-md/*.md` 读取怪物草稿，当前可读取 `1.md` 中的第一只怪物。
- `Main`、`GameRoot`、`Hud`、`HandView`、`CardView` 组成当前可启动的基础界面。

## 基础战斗规则

- 无圣遗物、装备和人物等级加成时，玩家初始血量为 50。
- 每回合开始时玩家获得 4 点费用，然后进入出牌阶段。
- 基础攻击力为 15；攻击牌的百分比伤害以该数值为基准。
- 护盾基础值为 10；格挡牌未设置具体数值时获得 10 点护盾。
- 怪物每回合有行动倒计时。玩家每打出一张牌，倒计时 -1。
- 玩家结束出牌阶段，或怪物倒计时归 0 时，怪物行动一次。
- 每回合怪物最多行动一次；若出牌阶段已经行动，结束出牌阶段时不会再次行动。
- 玩家生命值归 0 时进入“骗分状态”，不立刻失败，所有卡牌费用 +1。
- 骗分状态下再次被怪物攻击，战斗失败。
- 每个怪物有独特算法属性；受到对应算法属性攻击时，伤害翻倍。

## 存档

- 存档保存在本机 `user://saves/` 目录。
- 当前支持 5 个档位：`slot_1.json` 到 `slot_5.json`。
- 存档不加密，使用可读 JSON。
- 战斗状态变化时会实时保存当前档位。
- 首页提供三个主按钮：新旅途、读档、退出。
- 新旅途与读档都会进入 5 档选择界面；读档按钮会禁用空档位。

当前存档会记录玩家生命、费用、骗分状态、怪物状态、行动意图、手牌、抽牌堆、弃牌堆和删除区。

## 战斗界面

当前战斗界面按 `example.png` 参考图做了初步布局：

- 顶部为状态 HUD，包含玩家血量、护盾、费用、怪物意图。
- 中部为战场，左侧显示玩家，右侧显示怪物。
- 底部为手牌池。
- `example.png` 与 `example1.png` 已加入 `.gitignore`，不会进入仓库。

## 当前测试卡牌

当前测试卡牌来自 `cards-md/moni.md`：

- 名称：枚举
- 费用：1
- 类型：攻击
- 属性：模拟
- 效果：对单个敌人造成 100% 伤害

## 当前测试怪物

当前测试怪物来自 `monsters-md/1.md`：

- 名称：A+B Problem
- 血量：129
- 默认算法属性：模拟
- 基础攻击：3
- 普通攻击：行动值 5，对玩家造成 100% 伤害
- WA：行动值 7，对玩家造成 200% 伤害

怪物行动会按当前 md 中的攻击类型顺序轮换。当前实现中，A+B Problem 第一轮准备“普通攻击”，行动后下一次准备“WA”。

## Markdown 数据格式

卡牌目录为 `cards-md/`，怪物目录为 `monsters-md/`。导出预设会把这两个目录里的 `.md` 文件打进测试包。

怪物 md 当前支持字段：

```text
名字：A+B Problem
血量：129
属性：模拟
基础攻击：3

攻击类型：
1. 普通攻击：行动值5，对玩家造成100%伤害
2. WA：行动值7，对玩家造成200%伤害
```

## 后续建议

1. 定义第一批卡牌、敌人和遗物资源。
2. 完成抽牌、费用、弃牌、敌人意图和回合结算。
3. 增加地图、事件、奖励和存档。
4. 在 Godot 编辑器中生成导入缓存并运行主场景。
